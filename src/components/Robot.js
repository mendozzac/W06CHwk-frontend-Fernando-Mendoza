const Robot = ({ name, features }) => {
  return (
    <>
      <div>
        <img src="url" alt="robot"></img>
        <p>{name}</p>
        <p>{features}</p>
      </div>
    </>
  );
};

export default Robot;
